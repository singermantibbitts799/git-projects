let x = 10;
console.log(x);

const arr: number[] = [5, 7, 9];
arr.push(2);
console.log(arr);

function addNumbers(a: number, b: number): number {
    return a + b;
}

addNumbers(3, 4); // Call the function with two arguments
